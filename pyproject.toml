[project]
name = "lakehouse"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "dbt>=2.0.6",
    "dbt-osmosis>=1.5.0",
]

[tool.uv.workspace]
members = [
    "bills",
    "packages/*",
]

[dependency-groups]
dev-dependencies = ["ruff", "pyright", "pytest"]
